# X launch post — railyard (2026-08-06)

Status: draft for Claire's review. Option B (thread) recommended.

## Option A — single post

Your agent says the fix is done. Is it merged?

railyard turns "go build X" into a merged, verified change — routed to the
right model, the right machine, the right workflow. Not a claim. A receipt.

A plugin for Claude Code + Codex:
github.com/novotnyllc/railyard

## Option B — thread (recommended)

1/ Your agent says the fix is done. Is it merged?

Meet railyard: a plugin for Claude Code + Codex that turns "go build X"
into a merged, verified change. Not a claim. A receipt.

2/ "Done" means:
- merge commit reachable from main
- a real post-merge check, passed

Not "CI is green." An independent review pass gates every merge — the
model that wrote the code doesn't get to be the only one that checked it.

3/ Every subagent dispatch names an explicit model + effort — enforced by
a hook that refuses the call otherwise. Routine work can't quietly burn
your premium tier.

Fleet-aware too: work only goes to machines proven ready (its sibling,
roundhouse, keeps them that way — even Windows, launched through WSL).

4/ Install:
claude plugin marketplace add novotnyllc/marketplace
claude plugin install railyard@novotnyllc

Then just say what you want shipped.

Built on @every's compound-engineering plugin; ships adapted work from
@steipete (Oracle) and @cursor_ai (Thermos reviews) — credits in the docs.

## Hook alternates

- "Tests pass" and "done" are not the same claim.
- I stopped trusting "done" from my agents. So I built the thing that
  proves it. (founder-journey voice)

## Notes

- No hashtags: dev X engages on substance; hashtags read as marketing.
- Pull-quote in A/1 condenses docs/site/index.md's opening line so the
  landing page pays off the tweet.
- OG image: use the terminal-frame moment described in index.md's
  og-image comment (or assets/hero.jpg until it exists).
- Verify @cursor_ai / @every handles before posting.
