interface:
  display_name: "Task Orchestrator"
  short_description: "Coordinate requested fleet/account or remote-agent work."
  default_prompt: "Use $orchestrate for this explicitly requested allocation or remote-agent objective. Keep ordinary local work on native tools and subagents, give each scope one owner, and verify the requested result. Create visible user-owned tasks only on explicit user direction; CE owns any review settlement and CI monitoring."
