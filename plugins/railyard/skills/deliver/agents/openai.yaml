interface:
  display_name: "Deliver"
  short_description: "Complete a change with native work and selected CE."
  default_prompt: "Use $deliver to complete this change through the requested endpoint. Select useful CE stages automatically, preserve local or plan-only stops, and continue authorized shipping through CE-owned settlement, merge, and post-merge proof."
