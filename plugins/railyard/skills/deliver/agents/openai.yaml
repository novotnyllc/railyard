interface:
  display_name: "Deliver"
  short_description: "Route and complete one change lane."
  default_prompt: "Use $deliver to route this single lane, defaulting implementation delivery to LFG through merge and post-merge proof unless a narrower stop is explicit."
